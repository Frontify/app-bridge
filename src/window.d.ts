export declare global {
    interface Window {
        application: {
            config: {
                context: {
                    project: {
                        id: number;
                    };
                };
            };
        };
    }
}
